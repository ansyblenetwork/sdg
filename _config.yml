name: Soli Deo gloria
baseurl: solideogloria

permalink: /:year/:month/:day/:title
plugins:
  - jekyll-seo-tag
defaults:
  -
    scope:
      path: ""
      type: "posts"
    values:
      layout: "page"
